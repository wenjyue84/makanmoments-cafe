# AI providers
GROQ_API_KEY=gsk_xxx
OPENROUTER_API_KEY=sk-or-xxx

# Server
PORT=3031

# WhatsApp (optional — leave empty for website-only mode)
# Format: country code + number, no + (e.g. 60123456789)
WHATSAPP_NUMBER=
WHATSAPP_AUTH_DIR=./whatsapp-auth

# Knowledge base path (relative to backend dir)
KNOWLEDGE_DIR=../knowledge
